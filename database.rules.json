{
  "rules": {
    "groupLocations": {
      "$groupId": {
        "$userId": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid === $userId"
        }
      }
    }
  }
}
